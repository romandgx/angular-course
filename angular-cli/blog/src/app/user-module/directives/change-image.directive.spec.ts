import { ChangeImageDirective } from './change-image.directive';

describe('ChangeImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangeImageDirective();
    expect(directive).toBeTruthy();
  });
});
