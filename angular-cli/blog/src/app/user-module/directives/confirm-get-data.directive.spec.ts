import { ConfirmGetDataDirective } from './confirm-get-data.directive';

describe('ConfirmGetDataDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfirmGetDataDirective();
    expect(directive).toBeTruthy();
  });
});
